import SwiftUI

// ShopView — shops have been removed from the game.
// This stub is kept so any residual navigation references compile.
struct ShopView: View {
    let engine: GameEngine
    let shopIndex: Int

    var body: some View {
        EmptyView()
    }
}
